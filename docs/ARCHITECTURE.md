# Architecture

The source sessions stay read-only. The importer writes redacted Review notes, the reviewer controls approval, and the compiler writes managed blocks into formal knowledge. Claudian is an interface; the Obsidian Vault is the knowledge store.
